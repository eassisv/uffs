#include <iostream>
#include <vector>
using namespace std;

#define PAI(X) (X >> 1)
#define LEFT(X) (X << 1)
#define RIGHT(X) (X << 1 | 1)
#define vi vector<int>

template < class T, class Alloc = allocator<T> > struct MinHeap{
  vi heap;


};

int main(void){
  
  
  return 0;
}
